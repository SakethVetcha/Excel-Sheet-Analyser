{
  "name": "excel-sheet-analyser",
  "version": "1.0.0",
  "description": "Excel Sheet Analysis Tool",
  "main": "netlify/functions/app.js",
  "scripts": {
    "build": "pip install -r requirements.txt"
  },
  "dependencies": {
    "@netlify/functions": "^2.0.0",
    "@netlify/plugin-python": "^1.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
} 